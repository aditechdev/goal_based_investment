import 'package:flutter/material.dart';

class AppThemeSetup {
  /// The current will be used get the latest theme in the App.
  static ThemeData currentAppTheme() {
    return ThemeData(
      primarySwatch: Colors.blue,
    );
  }
}
